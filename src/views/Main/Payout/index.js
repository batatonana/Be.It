import { View, Text } from "react-native";
import { styles } from "./styles";

export const Payout = () => {
    return(
        <View style={styles.center}>
            <Text>Payout</Text>
        </View>
    );
};